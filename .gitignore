# ---- 提供的示例 / 作业材料（不入库）----
InfoSec-example-skills/
作业说明/
.claude/

# ---- 数据集 / 输出产物（体量大，不入库；从原始来源获取）----
data/
**/data/
outputs/
output/
out*/
raw/
normalized/
unified/
*.parquet
*.arrow

# ---- 模型权重 / 缓存 ----
models/
hf_cache/
huggingface/
.cache/
*.bin
*.safetensors
*.pt
*.pth
*.ckpt
*.gguf
*.onnx

# ---- 图片 / 截图（运行截图请放进小组报告/提交 zip，不入 git）----
*.png
*.jpg
*.jpeg
*.gif
*.bmp
*.webp
*.tiff
# 例外：guard-shieldgemma2 的合成 fixture 小图（<1KB、脚本可再生、非数据集 dump——M3_SPEC §0-5；
# 字节一致性由 tests/test_synth_images.py 锁定；真实数据集图像仍然全部不入库）
!guard-shieldgemma2/tests/fixtures/images/*.png
!guard-shieldgemma2/examples/images/*.png

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
env/
.python-version
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ---- 密钥 / 环境（绝不入库）----
.env
.env.*
*.key
secrets*
hf_token*
*token.txt

# ---- OS / 编辑器 ----
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# 注：M0_*.jsonl 是小体量手写样本，保留入库（不在上面被忽略的目录内）。
