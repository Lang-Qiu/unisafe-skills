# Core-Full only: the shieldgemma2 adapter (caption-rule + validate + metrics are stdlib-only).
# Lower bounds = versions verified in M3 task 3 regression gate (transformers 4.57.6 /
# accelerate 1.10.1 / bitsandbytes 0.48.2 on torch 2.5.1+cu121); <5 guards against a
# future transformers major. torch itself is intentionally NOT pinned here — install
# the build matching your CUDA (see pytorch.org).
transformers>=4.50,<5
accelerate>=1.10
bitsandbytes>=0.48
pillow>=10
