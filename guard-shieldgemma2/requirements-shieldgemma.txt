# Core-Full only: the shieldgemma2 adapter (caption-rule + validate + metrics are stdlib-only).
# transformers upper bound is torch-coupled (M3 task 4 measurement): 4.53+ rewrote the
# Gemma3 masking path to require torch>=2.6, so on torch 2.5.x stay <4.53 (verified
# working: 4.51.3). On torch>=2.6 the bound can be lifted to <5. torch itself is
# intentionally NOT pinned here — install the build matching your CUDA (pytorch.org).
# Verified quartet: torch 2.5.1+cu121 / transformers 4.51.3 / accelerate 1.10.1 / bitsandbytes 0.48.2.
transformers>=4.50,<4.53
accelerate>=1.10
bitsandbytes>=0.48
pillow>=10
