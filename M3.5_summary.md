# M3.5 交付总结 — Skill 性能优化（方向 B，乙）

> 日期：2026-06-16。契约：[`M3.5_SPEC.md`](M3.5_SPEC.md)；编排：[`tasks/plan-m3.5.md`](tasks/plan-m3.5.md) / [`tasks/todo-m3.5.md`](tasks/todo-m3.5.md)（7 任务全完成）。
> 范围：广义 skill **性能**（可复现性/触发/输出稳定性/运行效率），**非判别效果**（效果优化 E1-E3 后置 M3.6，见 §5）。
> 结论：**Cα ✅（Core 软门）｜Cβ ✅（M3.5 交付硬门）**；提交线 `462b694..f43f4bf`（5 个 commit）。

## 1. 追溯表（W × 实现 × 证据 × 状态）

| 工作项 | 内容 | 实现 | 证据 | 状态 |
|---|---|---|---|---|
| **W1 可复现性** | run_metadata 记 model_revision + 库版本 | 两侧 adapter 暴露 `config._commit_hash`；main.py `_probe_env(requested, revisions)` | 真跑 `model_revision=548e04f…86` 非 null（曾硬编码 null）+ `lib_versions{transformers 4.51.3, bnb 0.48.2, accelerate 1.10.1}`；rule/caption-rule 如实 null；`test_repro` ×2（8 测试，离线） | ✅ |
| **W2 触发** | description 扩"评估已有预测" + 文档一致 | 两侧 SKILL.md frontmatter + trigger-eval.md | 加"score/AUROC for existing predictions"（补 P8 缺口）；负例排除子句两侧未动；trigger-eval before/after 回归脚手架（after 待人工） | ✅（盲测=报告项→M4） |
| **W3 输出稳定性** | int8 run-to-run 方差 + flip_rate | sg2 `test_variance_smoke`（opt-in，N=5/K=3） | live **flip_rate=0 / 极差=0 / σ=0 → int8 完全确定性**；NaN/漂移=确定性伪影非随机噪声（notes §6.1b）；离线 skip | ✅ |
| **W4 运行效率** | judge 有界并发（取代 R7） | `llm_judge.predict_batch`（ThreadPool 按 id 回填）+ main.py `--judge-concurrency N`（默认 1） | TDD 6 测试（串行等价/顺序/守恒/提速/容量/parser）；live 12 条 **340s→88s=3.86×（74%↓）** + counts 守恒 + 逐 id 一致 + 无重排 | ✅ |

## 2. 真实数字

- **W1**：`env.model_revision = "548e04faf66e2234e6d113a71f530e1affa8dd86"`（镜像权重解析 commit）；`lib_versions` 三件套齐。
- **W3**：int8 同 5 图连跑 3 次，flip_rate **0.000**、yes-prob 极差 **0.000000**、policy σ **0.000000**——确定性。
- **W4**：`-j1` 340s → `-j4` 88s = **3.86×**（壁钟 −74%）；counts (predicted 12 / errors 0 / skipped 0) 两次逐项相同；12 条逐 id 判定一致。

## 3. Spec §10 验收 sweep

- [x] W1：两侧真跑 `model_revision` 非 null + 库版本齐；离线测试绿
- [x] W2 门控部分：description 覆盖指标评估措辞；trigger-eval 一致 + 新增正例；负例边界未动。人工盲测=报告项（trigger-eval §5 before/after，after 顺延 M4）
- [x] W3：opt-in 方差 smoke（N=5/K=3）产出方差 + flip_rate 并落 notes
- [x] W4：`-j1` 串行等价 + `-j4` 提速 3.86×（≥30% 硬门 ✓、≥2× 目标 ✓）+ success/errors/skipped 守恒 + R7 登记
- [x] 全局：套件绿（**llama 100 / sg2 99**，3 skip 为 opt-in live）；`git grep` 凭证/权重零命中；M2 偏差台账更新

## 4. 偏差登记

- **R7 superseded**（M2_SPEC「judge 不引入并发，Ask-first」→ 用户 2026-06-16 显式授权）：`--judge-concurrency` 默认 1=串行保证向后兼容；实测安全（守恒+逐 id 一致+无重排）。已落 `M2_summary.md` §5。
- **W1 SHA256**：§9-1 的可选权重 SHA256 未做（model_revision commit hash 已足够辨版本）；如需 M4 复现核对再补 config+首分片哈希。

## 5. 后置：判别效果优化（⭐ M3.6 重点，不在 M3.5）

效果杠杆 E1（应用校准阈值为默认）/ E2（栈升级修 int8 NaN）/ E3（NaN 回退）→ **M3.6 头号目标**，详见 [`M3.5_SPEC.md`](M3.5_SPEC.md) §12。结构上限（三策略天花板、judge 对抗鲁棒）报告里如实定位。

## 6. 人工待办（非技术验收）

- [ ] W2 触发**人工盲测复测**（description 调优后，新会话；trigger-eval §5 填 after；达标线正例 ≥7/8、负例 0）——与 M4 三方 trigger eval 同轮
- [ ] M4：报告 + 截图（含 M3.5 的 revision/方差/提速数字）+ 复现核对 + 打包 `学生1_学生2.zip`
