# 甲已干活

我是甲，目前我已经做完“待甲确认”中所有的任务，这是一篇发回原仓库的报告。

## 一、我都干了啥

我负责的主要包括：

- 把文本数据集转换成统一 JSONL，供文本 guard 评测使用
- 把图像数据集转换成统一 JSONL，供图像 guard 评测使用
- 保证输出格式符合仓库内统一接口约定
- 更新SKILL和README到一个能够执行的工作流流程，加入Harness

对应到本仓库，已完成的重点是：

- `dataset-wildguardmix`
- `dataset-unsafebench`

## 二、已完成事项总览

### 1. WildGuardMix 文本数据集 Unify

已完成 `dataset-wildguardmix` 的主要实现与检查：

- 完成 `references/category_mapping.json`
- 完成 `scripts/main.py`
- 支持 `wildguardtest` / `wildguardtrain`
- `wildguardtest` 会追加 XSTest safe probes，用于 over-refusal 评测
- 输出统一 JSONL 与 `metadata.json`
- 已完成全量跑数
- 已通过 `dataset-format-checker`

我这边对接乙时，文本侧已经可以提供真实 unified 数据，不再只是样例。

### 2. UnsafeBench 图像数据集 Unify

已完成 `dataset-unsafebench` 的主要实现与检查：

- 完成 `references/category_mapping.json`
- 完成 `scripts/main.py`
- 完成 `examples/output.sample.jsonl`
- 更新 `SKILL.md` 与 `README.md`
- 支持从 `yiting/UnsafeBench` 下载并转换为统一 JSONL
- 支持图片落盘，并在记录中写入相对路径
- 输出统一 JSONL 与 `metadata.json`
- 已通过 `dataset-format-checker`
- 图像集我已经私发了

## 三、目前的实际产物

### 1. WildGuardMix

文本产物已完成全量标注，1735 条，safe 样本居多，在测试集中加入了 XSTest 指针，人肉检查了没有问题，SKILL 优化我后面再做

### 2. UnsafeBench

多模态也已经全量标注完毕，总条数 2037 条，safe 样本居多，没有加 XSTest 指针，人肉检查后路径正确

### 3. 负例审核

使用GPT 5.4，思考等级 High，所有负例都没有触发，但 AUROC 的正例没有触发，更新了相应文档

### 4. Reviewing

我保证所有正样本中 `is_unsafe` 标签都是 `true`，代表有害样本。AUROC 我并没有测出来。其他的要求我都没意见。

## 四、存储位置

### 1. Dataset WildGuardMix

WildGuardMix 按仓库约定映射到统一 canonical taxonomy，并保留原始标签信息，就在原来的文件夹下。

### 2. Dataset UnsafeBench

UnsafeBench 的 11 类映射如下，如果你改了，我这好像有可能对不上：

- `Hate -> hate_discrimination`
- `Harassment -> harassment_bullying`
- `Violence -> violence`
- `Self-harm -> self_harm`
- `Sexual -> sexual_content`
- `Shocking -> general_harm`
- `Illegal activity -> illicit_behavior`
- `Deception -> misinformation`
- `Political -> political_persuasion`
- `Public and personal health -> medical_safety`
- `Spam -> other`

同样也在原文件夹。

