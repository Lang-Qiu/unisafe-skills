# 待甲确认事项（甲乙同步专用，跨里程碑）

> 乙 → 甲。**本文件是两人之间所有待确认/同步事项的唯一载体**——2026-06-12 由 `M1_待甲确认.md` 更名而来（条目编号连续累加：#1–#4 源于 M1，#5 起为 M2/M3 增量），后续新事项一律追加于此，不再新开确认文件。
> 背景：M1 主 Skill `guard-llama-guard` 规格见 [`M1_SPEC.md`](M1_SPEC.md)；M2/M3 增量见 [`M2_SPEC.md`](M2_SPEC.md) / [`M3_SPEC.md`](M3_SPEC.md)。
> 处理方式：**默认接受、否决式确认**——#1 自其推送起 **48 小时**内无异议即视为生效；其余各项按条目内时间窗。任何一条有异议，直接回复编号即可，不必整文 review。

## 当前行动清单（2026-06-12 更新：乙侧 M1/M2 技术交付均完成（[`M1_summary.md`](M1_summary.md) / [`M2_summary.md`](M2_summary.md)），M3 启动，#5 新增）

按优先级，各条详情见下方对应编号：

| 优先级 | 事项 | 要做什么 | 预计耗时 |
|---|---|---|---|
| **高** | **#4 metrics 公式 review** | 文件已推送 main，review 三个核对点 + 双口径；**这是乙侧唯一等你签字的验收项（§9-D）**，PR approve 或回"无异议"即算 | 15–20 分钟 |
| 中 | #2 数据就绪日期 | 回个预计日期即可；子集先到也行（M1 只用 `--limit 200`）。你的数据到位后乙一条命令重出真实指标+重截图 | 1 分钟 |
| 中 | #3 负例盲测 | 6 条负例已备好：`guard-llama-guard/references/trigger-eval.md` §2（N1–N6）。每条**开全新会话**提问，只记录是否误触发 guard skill，结果回乙。盲测要求：**先别看该文件 §1 的正例表** | 10 分钟 |
| 低 | #1 布局补充否决窗口 | 默认生效中（2026-06-11 推送起 48h，约至 06-13）；无异议**无需任何动作**，有异议回"#1 + 理由" | 0–5 分钟 |
| 低 | **#5 图像路径契约（M3 新增）** | 默认接受制（推送起 48h）；无异议**无需任何动作**，有异议回"#5 + 理由"或直接回 UnsafeBench 实际落盘布局 | 0–5 分钟 |

## #1 M0 契约补充：Guard 输出文件布局【已写入 `M0_接口约定.md` §5 末尾】

- **内容**：`<output_dir>/predictions/<guard>.predictions.jsonl` + `metrics/metrics.{json,md}` + `run_metadata.json`，附行数守恒式（predictions 行数 = total − skipped，error 行 `is_unsafe=null`）。
- **为什么需要**：M0 原来只定义了 Guard 输出的**记录** schema（§5），没定义**文件**落点；M2 多 Guard 对比、以及你按 `id` join 回数据集做交叉分析时，都需要一个稳定路径约定。
- **影响面**：只增不改——你已有的字段语义、checker 验收口径零变化。
- **请你做**：审 `M0_接口约定.md` §5 新增块；48h 内无异议即生效。

## #2 WildGuardTest unified 输出的就绪时间

- **问题**：乙的 Core-Full 联调（Llama Guard 第一版指标）与 E2E 截图需要你的 unified 输出，子集即可（M1 演示已定为 `--limit 200`，全量留 M2）。
- **请你做**：回个预计日期。若晚于乙的 C2 检查点，乙先用 `M0_dataset_unified_sample.jsonl` 顶替联调与截图，最终提交前用真实数据重截。
- **默认**：接受"样本顶替、提交前重截"方案。
- **状态（2026-06-11）**：默认方案已实际发生——C2/任务 13 用 examples+fixtures 顶替（10 条 eligible）出了第一版两 Guard 指标矩阵，已在 `M1_summary.md` §3 如实记录。你的数据就绪（需 dataset-format-checker exit 0）后，乙重跑一条命令即可换成真实数字。
- **状态（2026-06-12，M2）**：乙侧已升级为**三 Guard**（rule / llama-guard / llm-judge）× 按类别 × 对抗分桶 × 对比透视的完整流水线（`M2_summary.md`）。你的数据到位后全量重跑即出最终矩阵；唯一注意：llm-judge 约 22.5 秒/条，全量 1,725 串行 ≈11 小时——若时间紧，rule/llama 全量 + judge 子集会如实标 **partial**。

## #3 trigger eval 负例盲测（约 10 分钟，C1 之后任意时间）

- **问题**：需要验证 `guard-llama-guard` 不会被**数据集类请求**误触发（两边 skill 的 description 是互斥设计，你是最自然的盲测者）。
- **请你做**：拿到乙提供的 6 条负例 prompt（如"转换 WildGuardMix""检查这个数据集格式""下载 UnsafeBench"），在装好全部 skills 的**新会话**里逐条提问，记录是否误触发 guard skill，结果回给乙记入 `references/trigger-eval.md`。
- **默认**：愿意；具体时间你定。
- **状态（2026-06-11）**：负例集已就绪——`guard-llama-guard/references/trigger-eval.md` §2 的 N1–N6 共 6 条，可直接开测；盲测要求先不看该文件 §1 的正例表。
- **状态（2026-06-12，M2）**：该盲测已与 M2 任务 16（trigger eval 实测档）合并为同一轮——你测负例、乙测正例，结果分别记录；这是 M2 唯一等你配合的实测项。

## #4 metrics 公式交叉 review（对应 M1 验收 §9-D，C2 前后）【文件已就绪 2026-06-11，可直接 review】

- **请你做**：review `guard-llama-guard/references/metrics-definitions.md`（已推送 main；实现 `scripts/metrics.py` 与手算对照测试 `tests/test_metrics.py` 同在），重点核对三处：**正类=unsafe** 的方向、FPR 分母（FP+TN）、AUROC 的剔除规则（`is_unsafe=null` 全剔、`confidence=null` 仅剔 AUROC）。另增**双口径**定义（answered_only / failure_as_wrong，防 error 剔除虚高）请一并过目。PR approve 即算签字。
- 顺带：`references/category_mapping.json` 与你侧映射表的一致性（M0 §4 既有约定）一并扫一眼。
- **追加（2026-06-11，M2）**：`metrics-definitions.md` 末尾新增 **v2 段（§9–§12）**——按类别指标、对抗三态分桶、over-refusal 正式化、多 Guard comparison。纯新增不改 v1，**并入本条同一 review**；你若对 v1 三个核对点有异议，v2 同步修订。

## #5 M0 契约补充：图像记录的路径解析与缺图处理【M3 新增，2026-06-12 推送，48h 否决窗口】

- **背景**：M3 = 多模态亮点（你 `dataset-unsafebench` → 乙 `guard-shieldgemma2`）。M0 §3 已定图像真值与输入字段（`label.is_unsafe` + `content.images[].path`），但 path 的**解析基准**与缺图/异常处理 M0 未定义——乙侧实现前需锁定。完整规格见 [`M3_SPEC.md`](M3_SPEC.md) §4.1/§4.2。
- **内容（五点）**：
  1. **路径基准**：`content.images[].path` 为相对路径时，以 **unified JSONL 文件所在目录**为基准解析；绝对路径原样使用。你侧唯一约定动作：图片目录与 unified JSONL 的相对位置保持稳定（建议 `<unified_dir>/images/...`）。
  2. **eligible 判定**：`content.images` 非空且首图 `path` **或** `url` 至少一个非空即 eligible；两者均空 → `skipped.missing_content`（不写输出行）。
  3. **url-only**：只有 `url` 没有本地 `path` 的记录，乙侧不下载（read-only + 网络隔离）→ 记录级 error `image_url_not_supported`。请尽量把图片存盘并填 `path`。
  4. **缺图/坏图可见化**：`path` 文件不存在 → error `image_not_found`；文件损坏/非图像格式 → error `image_decode_error`。三类 error 均照常写输出行、计入 `counts.errors`、进双口径指标——**不静默跳过**，数据缺口在指标里可见。
  5. **多图记录**：M3 只评首图（UnsafeBench 为单图，此为防御性约定）；多图记录会被 warning 计数（run_metadata + 行级标注），**不算错误**。
- **为什么需要**：#1 的布局补充只覆盖了 Guard **输出**侧；图像**输入**侧的路径语义是甲乙文件交换的最后一处未定义点，M2 多 Guard 对比与 M3 跨模态评测都建立在它上面。
- **影响面**：只增不改——你已有字段语义、checker 验收口径零变化；text 侧完全不受影响。
- **请你做**：无异议**无需任何动作**（默认接受，自本条推送起 48h，约至 06-14）；有异议回"#5 + 理由"。若 UnsafeBench 实际落盘布局与 ① 不一致，直接回实际布局即可——乙侧解析基准是单函数修改，改起来很快。

---

| 条目 | 时间窗 | 默认结果 | 状态（2026-06-12） |
|---|---|---|---|
| #1 布局补充 | 推送后 48h | 生效 | 否决窗口进行中（约至 06-13） |
| #2 就绪日期 | 本周内回复 | 顶替方案生效 | 顶替已发生并记录；等日期 |
| #3 盲测 | C1 后任意 | 愿意 | 负例集已备好，可随时开测（与乙 M2 任务 16 / M3 图文互斥测同轮） |
| #4 公式 review | C2 前后 | —（需实际动作，无默认） | **文件已就绪，等你签字（§9-D 唯一未勾项）** |
| #5 图像路径契约 | 推送后 48h | 生效 | 否决窗口进行中（约至 06-14） |
