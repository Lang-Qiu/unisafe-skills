"""guard-llama-guard: unified text safety guard skill.

Package name uses underscores (guard_llama_guard); the skill directory keeps
the hyphen. Entry points: ``guard_llama_guard.main`` (run guards) and
``guard_llama_guard.metrics`` (evaluate guard outputs against truth labels).
"""

__version__ = "0.1.0"
