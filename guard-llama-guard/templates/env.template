# Environment template for guard-llama-guard (copy values into your shell or .env;
# .env files are gitignored — NEVER commit real keys).

# --- Core-Full: Llama Guard 3 (gated; direct huggingface.co connection required) ---
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxx
# Optional download cache reuse:
# HF_HOME=E:\hf_cache

# --- Mirror (PUBLIC repos only; gated repos are NOT served by hf-mirror) ---
# HF_ENDPOINT=https://hf-mirror.com

# --- Extension E1: LLM-as-judge (OpenAI-compatible endpoint) ---
# LLM_API_KEY=sk-xxxxxxxx
# LLM_BASE_URL=https://your-endpoint/v1
# LLM_MODEL=your-model-name
