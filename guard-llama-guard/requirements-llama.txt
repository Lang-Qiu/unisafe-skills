# Core-Full: Llama Guard 3-1B (local, gated). Exact pins are recorded in metadata.json at runtime.
# torch is NOT pinned exactly: on Windows the CUDA wheel must come from the official index, e.g.
#   pip install "torch>=2.2" --index-url https://download.pytorch.org/whl/cu121   (match your CUDA)
# Installing torch from default PyPI on Windows yields a CPU-only wheel.
torch>=2.2
transformers>=4.43,<5
accelerate>=0.30
